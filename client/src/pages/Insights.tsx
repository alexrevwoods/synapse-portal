import { Link, useRoute } from "wouter";
import { ArrowLeft, BarChart3, Eye, GitFork, MessageCircle, MousePointer2, Network, Send, Sparkles, UsersRound } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import ProfileSwitcher from "@/components/ProfileSwitcher";

function Metric({ icon: Icon, label, value, accent = "cyan" }: { icon: typeof Eye; label: string; value: number; accent?: "cyan" | "violet" | "slate" }) {
  const color = accent === "violet" ? "text-violet-200 bg-violet-300/[.08]" : accent === "slate" ? "text-slate-300 bg-white/[.04]" : "text-cyan-100 bg-cyan-300/[.08]";
  return <div className="rounded-2xl border border-white/[.08] bg-white/[.02] p-4"><span className={`grid h-8 w-8 place-items-center rounded-lg ${color}`}><Icon size={15} /></span><p className="mt-5 text-2xl font-semibold tracking-[-.05em] text-white">{value.toLocaleString()}</p><p className="mt-1 text-[10px] font-extrabold uppercase tracking-[.12em] text-slate-500">{label}</p></div>;
}

export default function Insights() {
  const { isAuthenticated, loading } = useAuth();
  const [, params] = useRoute("/insights/:profileId");
  const profileId = Number(params?.profileId);
  const analytics = trpc.analytics.overview.useQuery({ profileId }, { enabled: isAuthenticated && Boolean(profileId) });

  if (loading) return <main className="min-h-screen bg-[#080B14]" />;
  if (!isAuthenticated) return <main className="grid min-h-screen place-items-center bg-[#080B14] px-4"><div className="max-w-md rounded-3xl border border-white/10 bg-slate-900/60 p-8 text-center"><BarChart3 className="mx-auto text-cyan-100" /><h1 className="font-display mt-5 text-2xl font-semibold text-white">Identity, with signal.</h1><p className="mt-3 text-sm leading-6 text-slate-400">Sign in to see real Portal, Node, social, and Network activity for your Profiles.</p><button onClick={() => startLogin()} className="primary-button mt-7 w-full">Sign in to continue</button></div></main>;
  if (analytics.isLoading) return <main className="min-h-screen bg-[#080B14] p-10 text-sm font-bold text-slate-500">Loading analytics…</main>;
  if (!analytics.data) return <main className="grid min-h-screen place-items-center bg-[#080B14] px-4 text-white"><div className="text-center"><p className="font-display text-2xl font-semibold">Profile not found</p><Link href="/onboarding" className="primary-button mt-6">Create a Profile</Link></div></main>;

  const { profile, overview, activity, nodeOpens } = analytics.data;
  return <main className="min-h-screen bg-[#080B14] text-white"><header className="sticky top-0 z-30 border-b border-white/[.07] bg-[#080B14]/85 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8"><div className="flex items-center gap-3"><Link href={`/builder/${profileId}`} className="flex items-center gap-2 text-xs font-bold text-slate-400 transition hover:text-cyan-100"><ArrowLeft size={14} /> Builder</Link><span className="h-4 w-px bg-white/10" /><span className="font-display text-sm font-semibold text-white">Insights</span></div><ProfileSwitcher activeProfileId={profileId} destination={(id) => `/insights/${id}`} /></div></header><div className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8 lg:py-12"><span className="eyebrow"><span className="signal-dot" /> Real activity, not vanity</span><div className="mt-5 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="font-display text-3xl font-semibold tracking-[-.055em] text-white">How @{profile.username} is moving.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">A direct read on discovery, exploration, conversation, and relationship growth. Data reflects recorded activity in this Portal.</p></div><Link href={`/${profile.username}`} target="_blank" className="secondary-button !px-3 !py-2.5 !text-xs"><Eye size={14} /> View Portal</Link></div><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Metric icon={Eye} label="Portal views" value={overview.portalViews} /><Metric icon={MousePointer2} label="Node opens" value={overview.nodeOpens} accent="violet" /><Metric icon={Sparkles} label="Signal reactions" value={overview.reactions} /><Metric icon={MessageCircle} label="Conversations" value={overview.comments} accent="slate" /></div><div className="mt-6 grid gap-6 lg:grid-cols-[minmax(0,1.7fr)_minmax(16rem,.7fr)]"><section className="rounded-2xl border border-white/[.08] bg-white/[.02] p-5 sm:p-6"><div className="flex items-start justify-between gap-4"><div><p className="font-display text-lg font-semibold text-white">Discovery over the last 7 days</p><p className="mt-1 text-xs leading-5 text-slate-500">Portal views and node exploration events by day.</p></div><span className="rounded-full border border-white/[.08] px-2.5 py-1 text-[9px] font-extrabold uppercase tracking-[.12em] text-slate-500">Live data</span></div><div className="mt-6 h-64"><ResponsiveContainer width="100%" height="100%"><AreaChart data={activity} margin={{ top: 6, right: 4, left: -26, bottom: 0 }}><defs><linearGradient id="viewGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#77e6fb" stopOpacity={0.35} /><stop offset="100%" stopColor="#77e6fb" stopOpacity={0} /></linearGradient><linearGradient id="nodeGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#a99cff" stopOpacity={0.25} /><stop offset="100%" stopColor="#a99cff" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="rgba(255,255,255,.07)" vertical={false} /><XAxis dataKey="label" stroke="#536074" tickLine={false} axisLine={false} tick={{ fontSize: 11 }} /><YAxis allowDecimals={false} stroke="#536074" tickLine={false} axisLine={false} tick={{ fontSize: 11 }} /><Tooltip contentStyle={{ background: "#111827", border: "1px solid rgba(255,255,255,.12)", borderRadius: 12, fontSize: 12 }} labelStyle={{ color: "#94a3b8" }} /><Area type="monotone" dataKey="views" name="Portal views" stroke="#77e6fb" strokeWidth={2} fill="url(#viewGradient)" /><Area type="monotone" dataKey="nodeOpens" name="Node opens" stroke="#a99cff" strokeWidth={2} fill="url(#nodeGradient)" /></AreaChart></ResponsiveContainer></div></section><aside className="rounded-2xl border border-white/[.08] bg-slate-950/35 p-5"><p className="font-display text-lg font-semibold text-white">Identity health</p><div className="mt-5 space-y-4"><div className="flex items-center justify-between border-b border-white/[.07] pb-3"><span className="flex items-center gap-2 text-xs font-bold text-slate-400"><Send size={14} className="text-cyan-100" /> Signals published</span><span className="text-sm font-semibold text-white">{overview.signals}</span></div><div className="flex items-center justify-between border-b border-white/[.07] pb-3"><span className="flex items-center gap-2 text-xs font-bold text-slate-400"><UsersRound size={14} className="text-cyan-100" /> Followers</span><span className="text-sm font-semibold text-white">{overview.followers}</span></div><div className="flex items-center justify-between border-b border-white/[.07] pb-3"><span className="flex items-center gap-2 text-xs font-bold text-slate-400"><Network size={14} className="text-violet-200" /> Connections</span><span className="text-sm font-semibold text-white">{overview.connections}</span></div><div className="flex items-center justify-between"><span className="flex items-center gap-2 text-xs font-bold text-slate-400"><Eye size={14} className="text-cyan-100" /> Signal views</span><span className="text-sm font-semibold text-white">{overview.signalViews}</span></div></div></aside></div><section className="mt-6 rounded-2xl border border-white/[.08] bg-white/[.02] p-5 sm:p-6"><div><p className="font-display text-lg font-semibold text-white">What visitors explore</p><p className="mt-1 text-xs text-slate-500">Node open events, ordered by actual visitor behavior.</p></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{nodeOpens.map((node) => <div key={node.id} className="rounded-xl border border-white/[.07] bg-slate-950/30 p-4"><p className="text-sm font-bold text-slate-200">{node.title}</p><p className="mt-3 text-2xl font-semibold tracking-[-.05em] text-cyan-100">{node.opens}</p><p className="mt-1 text-[10px] font-extrabold uppercase tracking-[.1em] text-slate-600">opens</p></div>)}{nodeOpens.length === 0 && <p className="text-sm text-slate-500">Add Nodes to begin measuring exploration paths.</p>}</div></section></div></main>;
}
