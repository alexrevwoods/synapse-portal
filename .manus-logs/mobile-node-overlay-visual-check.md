# Mobile Node overlay verification

The published Media Revolution Portal was opened and an existing Node was selected through the public graph. The selection updated its detail content and destination correctly on the desktop side panel, preserving the established desktop behavior. The new mobile implementation uses the same selected-Node state but hides the old beneath-map card at the mobile breakpoint; it presents the Node detail as an in-map overlay, and a repeat tap, Map action, chevron, or Escape dismisses it for continued graph exploration.

The final 390×844 view also confirms the compact Public Portal header no longer overlaps the Synapse wordmark with the Discover action. The map remains clear and fills the available mobile canvas before a Node is selected; the prior below-map detail card is absent.

The 390×844 public-map verification still shows a clear, full-width exploration canvas with no persistent detail card beneath it. The Builder verification preserves the compact mobile navigation and fluid Profile form after the expanded Node taxonomy was added. The focus indicator, pulsing overlay, and drag handle are interaction-dependent and appear only after a Node is opened, preserving a quiet map at rest.
