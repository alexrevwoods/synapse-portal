# Neural access visual verification

At the preview URL, `/access?next=/account` renders the supplied neural-access direction as a responsive Synapse-branded screen: liquid cyan, violet, and lime background fields; the existing Synapse mark; clear secure-session explanation; a dedicated identity field; and an OAuth-triggering access action. The real production domain is still serving its previous checkpoint and therefore does not yet include the new `/access` route; this is expected until the pending implementation is checkpointed/deployed.
